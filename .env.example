# Veritabanı Bağlantısı
# PostgreSQL veritabanı bağlantı URL'i
# Format: postgresql://username:password@host:port/database
DATABASE_URL="postgresql://muhasebe:muhasebe123@localhost:5432/muhasebe_db"

# NextAuth Ayarları
# NextAuth için güvenli secret key (mutlaka değiştirin!)
# Üretmek için: openssl rand -base64 32
NEXTAUTH_SECRET="change-this-secret-in-production"

# NextAuth URL'i
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXTAUTH_URL="http://localhost:3000"

# Node Environment
# development veya production
NODE_ENV="development"

# Docker PostgreSQL Ayarları (Docker kullanıyorsanız)
POSTGRES_USER="muhasebe"
POSTGRES_PASSWORD="muhasebe123"
POSTGRES_DB="muhasebe_db"

# Docker Seed Ayarları (Opsiyonel)
# true: Seed verilerini yükle, false: Yükleme
RUN_SEED="true"

# Cron Job Secret (Opsiyonel)
# Cron job'lar için güvenlik secret'i
# CRON_SECRET="your-cron-secret-here"
